<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <title>Update Author</title>
</head>

<body>
  <header>
    <h1><a href="/">Writer's Library Hub</a></h1>
    <nav>
      <div>
        <a href="/books">Explore</a>
        <a href="/books/author/<%= user.id %>">My Books</a>
        <a href="/books/add">Add New Book</a>
      </div>
      <form class="nav-form" action="/logout" method="post">
        <a href="/authors/<%= user.id %>/profile">
          <%= user.name %>
        </a>
        <button>Logout</button>
      </form>
    </nav>
  </header>
  <section>
    <h1>Update Your Details</h1>
    <form class="form" action="/authors/<%= author.id %>/update" method="POST">
      <div class="form-group">
        <label for="name">Author Name</label>
      <input type="text" name="name" id="name" value="<%= author.name %>">
      </div>
      <div class="form-group">
        <label for="email">Email</label>
      <input type="email" name="email" id="email" value="<%= author.email %>" readonly>
      </div>
      <div class="form-group">
        <label for="phone">Phone Number</label>
      <input type="text" name="phone" id="phone" value="<%= author.phone %>">
      </div>
      <button type="submit">Confirm Update</button>
    </form>
  </section>

</body>

</html>